import { createTRPCRouter } from "@/trpc/init";

// TODO: Implement admin pickup addresses procedures
// This is a placeholder for future admin pickup addresses functionality
export const adminPickupAddressesRouter = createTRPCRouter({
  // Future procedures can be added here:
  // getPickupAddresses: adminProcedure.query(async () => { ... }),
  // createPickupAddress: adminProcedure.mutation(async ({ input }) => { ... }),
  // updatePickupAddress: adminProcedure.mutation(async ({ input }) => { ... }),
  // deletePickupAddress: adminProcedure.mutation(async ({ input }) => { ... }),
});
