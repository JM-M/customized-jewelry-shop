import { createTRPCRouter } from "@/trpc/init";

// TODO: Implement admin packaging procedures
// This is a placeholder for future admin packaging functionality
export const adminPackagingRouter = createTRPCRouter({
  // Future procedures can be added here:
  // getPackaging: adminProcedure.query(async () => { ... }),
  // createPackaging: adminProcedure.mutation(async ({ input }) => { ... }),
  // updatePackaging: adminProcedure.mutation(async ({ input }) => { ... }),
  // deletePackaging: adminProcedure.mutation(async ({ input }) => { ... }),
});
